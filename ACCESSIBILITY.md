# DOT BLOCKS 접근성 설계 명세

## 제품 기준

이 게임의 성공 기준은 화면 접근성 점수만이 아니라 다음 과업을 **눈을 가린 상태에서 DotPad와 소리만으로 수행할 수 있는가**입니다.

1. 게임 모드 선택과 시작
2. 현재 블록 모양 파악
3. 좌우 위치와 회전 상태 파악
4. 예상 착지 지점 확인
5. 고정 블록의 표면과 빈 공간 탐색
6. 줄 삭제, 점수, 위험 높이 인지
7. 일시정지와 재시작

## 정보 우선순위

1. 촉각 그래픽: 보드의 공간 구조
2. 짧은 방향 이어콘: 반복 조작의 즉각 피드백
3. 요청형 음성: 상세 상태와 수치
4. 배경음악: 분위기와 속도감만 제공

음성 중에는 배경음악을 자동 감쇠하며, 음성·효과음·음악을 독립적으로 끌 수 있습니다.

세 가지를 한 번에 끄는 소리 토글은 설정 서랍 안이 아니라 인트로와 컨트롤 도크에 상시 노출됩니다. 조용히 해야 하는 상황에서 서랍을 열고 항목을 찾는 단계 없이 즉시 끌 수 있어야 하기 때문입니다. 다시 켤 때는 끄기 직전의 개별 상태를 복원하므로, 음악만 꺼둔 사용자가 전체 토글을 썼다고 해서 음악이 다시 켜지지 않습니다. 버튼은 `aria-pressed`와 함께 현재 상태·다음 동작을 모두 담은 라벨("음성 켜짐, 누르면 끔")을 제공합니다.

## 촉각 부호

촉각 강도는 **정보 중요도 순서**를 따릅니다. 손이 가장 먼저 찾아야 하는 것은 지금 조작 중인
블록이므로, 현재 블록이 가장 강하고 고정 블록이 가장 약합니다.

### 낙하 모드 (셀당 4×3핀)

- 현재 블록: 셀 전체를 빈틈 없이 채움(12/12핀). 인접한 셀끼리 이음선 없이 붙어 조각 하나가
  연속된 한 덩어리로 만져짐
- 고정 블록: 셀 중앙의 작은 2×2 점(4핀). 사방에 여백이 남아 칸마다 끊겨 읽히고, 손을 훑으면
  울퉁불퉁한 표면으로 느껴짐
- 예상 착지: 셀 중앙을 가로지르는 가로선. 조각의 폭만큼 이어져 착지할 줄이 한 줄의 선으로 읽힘
- 위험 경고: 최상단 영역 좌우에 간헐적 경고점
- 표면 스캔: 각 열에서 가장 높은 블록의 윗면만 표시

4×3핀에서 테두리 질감은 성립하지 않습니다. 12핀 중 10핀이 올라가고 안쪽 2핀만 내려가므로
손끝에서는 꽉 찬 면과 구별되지 않고, 인접 셀의 테두리가 이중으로 겹쳐 조각 모양이 아니라
울퉁불퉁한 덩어리로 읽힙니다. 그래서 테두리는 쓰지 않습니다.

### 퍼즐 모드 (셀당 5×5핀)

- 선택 블록: 셀 전체 채움(25핀)
- 고정 블록: 셀 중앙 3×3(9핀)
- 배치 불가: X자

### 스폰 위치

블록은 항상 보드 안(0행)에서 온전한 모양으로 시작합니다. 보드 위쪽에서 걸쳐 내려오면
촉각 출력은 y<0인 부분을 그리지 않으므로 T는 아래 한 칸만, O·S·Z·J·L은 절반만 만져집니다.
절반만 만져지는 블록은 모양을 알 수 없기 때문에 난이도와 무관하게 전부 보드 안에서 시작합니다.

## 낙하 모드 보조 규칙

- 기본 속도는 `여유`입니다.
- 촉각 친화 잠금 유예 기본값은 켜짐입니다.
- 바닥에 닿은 순간 짧은 저음이 재생됩니다.
- 잠금 유예 중 좌우 이동 또는 회전이 가능합니다.
- 잠금 유예 연장에는 상한이 있습니다. 이동·회전으로 유예를 다시 시작할 수 있는 횟수는 8회,
  바닥에 처음 닿은 뒤 총 유예 시간은 잠금 지연의 3배까지입니다. 촉각 플레이는 만져보고 조금
  움직이고 다시 만져보는 반복이라, 상한이 없으면 블록이 바닥에서 영원히 고정되지 않습니다.
- 마지막 연장에 도달하면 "곧 고정됩니다"를 음성으로 알립니다.
- 표면 스캔을 열면 낙하가 자동으로 멈춥니다.
- 위험 높이는 2단계로 구분합니다.

## 음성 상태 문장

낙하 모드의 현재 상태는 다음 순서로 읽습니다.

1. 블록 이름과 가로/세로 방향
2. 점유하는 열 범위
3. 현재 행
4. 착지까지 남은 칸
5. 예상 바닥 행
6. 점수, 줄, 레벨
7. 다음/보관 블록

상세 스캔은 10개 열 높이와 내부 빈칸 수를 추가합니다.

## 스크린리더

- 상태 영역: `role=status`, `aria-live=polite`
- 위험과 게임 종료: 별도 `role=alert`, `aria-live=assertive`
- Canvas: 현재 게임 상태를 반영하는 동적 `aria-label`
- 모든 설정: 실제 button/select 요소 사용
- 시각 초점: 키보드 입력 시 3px 고대비 포커스 표시
- 모션 감소 설정 존중
