# DOT BLOCKS UI / Asset Guide

## 제작 원칙

| 요소 | 구현 방식 | 이유 |
|---|---|---|
| 인트로 배경 | WebP 이미지 | 분위기와 세계관을 한 장으로 표현하면서 용량 절감 |
| 플레이 배경 | CSS 그라디언트 + 도트 패턴 | 인트로와 시각적으로 구분하고, 보드 아트워크와 경쟁하지 않도록 |
| 보드 베젤 | WebP 이미지 (`board-frame.webp`) | 닷패드 실물 프레임. 캔버스와 정합돼야 하므로 배경이 아닌 보드 셸 요소로 사용 |
| 게임명·설명·점수·버튼 | HTML/CSS | 다국어, 스크린리더, 확대, 포커스 대응 |
| 기본 블록과 게임 보드 | Canvas | 실시간 이동·회전·삭제·반응형 크기 처리 |
| 특수 아이템 심볼 | 투명 SVG | 크기 변경 시 선명하며 CSS 블록 위에 합성 가능 |
| 닷패드 출력 | 40×60 좌표 데이터 | 화면 디자인과 독립적으로 촉각 정보 보장 |

## 화면 흐름

1. **Intro** — 모드 선택과 시작, 선택적 DotPad 연결
2. **Play** — 상단 상태 → 중앙 보드 → 하단 조작 안내 → 컨트롤 도크의 세로 Flex 구조.
   데스크톱은 좌(다음 블록)·중앙(보드)·우(점수) 3열, 모바일은 보드 위 가로 HUD로 재배치
3. **Settings drawer** — 조작법·기능키·소리·접근성·방향·핀 미리보기
   (음성·효과음·배경음악을 한 번에 끄는 소리 토글은 인트로와 컨트롤 도크에 상시 노출)
4. **Pause** — 계속하기 또는 인트로 복귀
5. **Result** — 최종 점수, 다시 플레이 또는 다른 모드 선택

## 기본 블록 규칙

기본 블록 이미지를 개별 PNG로 만들지 않습니다. 블록 종류는 좌표 배열이며 각 셀은 Canvas로 렌더링합니다. 색상은 시각적 보조 수단일 뿐이고, DotPad에서는 고정/현재/착지/배치 불가 상태를 서로 다른 핀 질감으로 표현합니다.

## 특수 아이템 추가 방법

`assets/items/`의 SVG를 블록 셀 위에 시각적으로 합성하되 실제 효과와 촉각 부호가 정의되기 전에는 플레이에 노출하지 않습니다. 새 아이템에는 반드시 다음을 함께 정의합니다.

- 시각 심볼과 대체 텍스트
- 획득 및 사용 효과음
- 음성 명칭과 효과 설명
- DotPad 촉각 부호
- 키보드·기능키 사용 방식
- 색상 없이도 구분되는 상태 표시
