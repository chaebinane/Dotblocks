# DOT BLOCKS Blind-first QA

## 실기 6차 (모니터 · 변화량 비례 대기) — 검증 결과

- `?monitor=1`로 자동 열림, 설정 버튼 `aria-pressed` 반영, `M` 키 토글 동작 확인
- `decodeHardware()`가 600자 hex를 40×60으로 복원. 모니터가 실제 전송 프레임과 게임판의
  불일치를 핀 단위로 집계하는 것 확인 (4줄 삭제 직후 512핀 다름을 정확히 표시)
- `요약 복사`가 모드·속도·회전·`STACK_TEXTURE`·불일치 핀 수·전송 통계·점자 두 줄·전송 로그를
  담은 텍스트를 생성하는 것 확인
- 4줄 동시 삭제 타임라인 (실시간 clock, setTimeout 실동작):

  | 경과 | 전송 | 대기 프레임 | 핀 == 보드 |
  |---|---|---|---|
  | +100ms | 1장 | 없음 | 일치 |
  | +300ms | 1장 | 있음 | 불일치 |
  | +600ms | 2장 | 없음 | 일치 |
  | +900ms | 2장 | 없음 | 일치 |
  | +1400ms | 2장 | 없음 | 일치 |

  전송 시각 +12ms, +523ms. 큰 변화(2400핀)에 대기시간 520ms가 적용됨
- 런타임 오류 0건, 캔버스 렌더 정상
- 1~5차 회귀 없음

### 실제 닷패드에서 확인이 필요한 항목

1. **이번 라운드의 핵심**: 여러 줄을 한 번에 없앤 뒤 모니터의 `불일치`가 몇 ms 안에 `일치`로
   돌아오는지. 오래 걸리면 `PAD_BIG_INTERVAL`을 올리고, 과하게 느리면 내립니다
2. `PAD_BIG_DIFF`(240핀) 경계가 적절한지 — 이보다 작은 변화도 밀리는지 모니터 로그의
   `변경 핀 수`와 체감을 대조
3. 모니터의 `합쳐짐`·`중복차단` 횟수가 과하지 않은지 (과하면 조작이 뭉개진다는 뜻)
4. 점자 줄이 그래픽보다 더 밀리는지 — 두 줄(`점자(전송)` / `점자(현재)`)을 비교

## 실기 5차 피드백 (핀 전송 폭주) — 검증 결과

jsdom에서 SDK를 가로채 `displayGraphicData()` 호출을 전수 기록해 수정 전후를 대조했습니다.

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 줄 삭제 1회당 전송 프레임 | 6장 (서로 다른 내용 3종) | 2장 |
| 좌우 왕복 이동 10회 (최종 위치 동일) | 10장 | 0장 (중복 차단) |
| 200ms 간격 조작 6회 | — | 6장 즉시 전송 (밀림 없음) |
| 즉시 8회 연속 조작 | — | 1장으로 합쳐짐 |
| 합쳐진 마지막 프레임 == 실제 보드 | — | 일치 |

- 최종 상태가 유실되지 않는 것이 핵심입니다. 간격 안에 들어온 프레임은 버리는 것이 아니라
  보관 후 내보내므로, 마지막 프레임이 누락되어 핀이 옛 상태로 굳는 경로가 없습니다
- 인라인 스크립트 `node --check` 통과, 런타임 오류 0건
- 1~4차 수정 회귀 없음: 스폰 y=0, 좌우 조작 시 4.5초 고정, 무조작 2.6초 고정,
  내리기 2회로 확정, 스택 빈 핀 0개

### 실제 닷패드에서 확인이 필요한 항목

1. 줄 삭제 후 핀이 화면과 같은 시점에 갱신되는지 (이번 수정의 핵심 확인 항목)
2. `PAD_MIN_INTERVAL` 170ms가 조작 반응으로 충분히 빠른지. 느리면 낮추고, 여전히 밀리면
   올리면 됩니다 — 값 하나로 조절됩니다
3. 빠른 낙하(도전 난이도)에서 중력 이동이 합쳐져 블록이 뛰어내리는 것처럼 느껴지지 않는지
4. 재탐색·고정 확인 펄스의 늘어난 대기(200/170/220ms)가 여전히 펄스로 느껴지는지

## 실기 4차 피드백 (빈틈 제거 · 내리기 확정) — 검증 결과

| 항목 | 결과 |
|---|---|
| 낙하 셀 고정 대 현재 | 12핀 대 12핀 (동일) |
| 퍼즐 셀 고정 대 현재 | 25핀 대 25핀 (동일) |
| 스택 2층 × 4칸 빈 핀 | 0개 |
| 연습(턴제) 내리기 2회 → 확정 | OK |
| 기본(촉각 저속) 내리기 2회 → 확정 | OK |
| 첫 누름에 조기 확정 | 없음 |
| 안내 음성 "내리기를 한 번 더" | 두 모드 모두 출력 |
| 턴제 20초 방치 시 자동 고정 | 없음 (기존 동작 유지) |

- 인라인 스크립트 `node --check` 통과, jsdom 실행 시 런타임 오류 0건
- 1차 수정 회귀 없음: 250ms마다 좌우 조작 시 4.5초 고정, 무조작 2.6초 고정
- `STACK_TEXTURE`를 `'layers'`/`'pieces'`로 바꿔도 문법·렌더 경로가 살아 있음을 확인

### 실제 닷패드에서 확인이 필요한 항목

1. 스택과 현재 블록이 촉각으로 완전히 같아진 상태에서, 블록이 스택 위에 얹혔을 때 위치를
   놓치지 않는지. 놓친다면 F1+F3을 얼마나 자주 쓰게 되는지
2. 고정 확인 펄스 150ms가 실제로 감지되는지. 이제 이것이 "내 블록이 어디 놓였는지" 알려주는
   유일한 즉시 신호입니다
3. 착지 가로선 한 줄이 꽉 찬 면 사이에서 충분히 구별되는지
4. 내리기 두 번으로 확정하는 흐름이 자연스러운지, 아니면 한 번에 바로 놓이는 게 나은지

## 실기 3차 피드백 (고정 조각 경계 · 고정 펄스) — 검증 결과

- 19행에 같은 조각 4칸과 서로 다른 조각 4칸을 나란히 배치해 핀 프레임을 덤프:
  같은 조각 구간은 15핀 연속(이음선 0), 다른 조각 구간은 3핀마다 틈
  `OOOOOOOOOOOOOOO.OOO.OOO.OOO.OOO.`
- 빈 핀 열 위치가 15, 19, 23, 27, 31 — 조각 경계마다 정확히 1핀
- 2줄 동시 삭제 후 `dBoard`와 `dPieceId`의 채워짐 여부를 200칸 전수 대조: 불일치 0건,
  행 수 20 유지. 두 배열이 어긋나지 않음을 확인
- `padLockPulse()`는 줄이 삭제되지 않은 경우에만 호출되며 프레임 2장(강조 1 + 복귀 1).
  줄 삭제 시에는 기존 플래시만 동작
- 인라인 스크립트 `node --check` 통과, jsdom 실행 시 런타임 오류 0건
- 1~2차 수정 회귀 없음: 스폰 y=0, 250ms마다 좌우 조작 시 4.5초 고정, 무조작 2.6초 고정

### 실제 닷패드에서 확인이 필요한 항목

1. 조각 경계 1핀 틈이 층 사이 틈(같은 1핀)과 혼동되지 않는지. 세로는 층, 가로는 조각 경계라는
   구분이 손끝에서 자연스럽게 잡히는지
2. 조각 경계가 생기면서 스택 윗면 윤곽이 다시 끊겨 느껴지지는 않는지 (2차 수정의 목적과 상충)
3. 고정 펄스 150ms가 손이 보드에 얹혀 있을 때 실제로 감지되는지, 너무 짧지 않은지
4. 조각이 많이 쌓였을 때 경계 틈이 너무 많아져 오히려 어수선하게 느껴지지 않는지

## 실기 2차 피드백 (쌓인 블록을 이어진 면으로) — 검증 결과

| 항목 | 1차 수정 | 2차 수정 |
|---|---|---|
| 낙하 셀 고정 핀 수 | 4핀 (중앙 2×2) | 8핀 (4×2, 폭 전체) |
| 퍼즐 셀 고정 핀 수 | 9핀 (중앙 3×3) | 15핀 (5×3, 폭 전체) |
| 현재 대 고정 대비 | 3.0배 / 2.8배 | 1.50배 / 1.67배 |
| 가로 인접 칸 연속성 | 칸마다 끊김 | 5칸 20핀 전부 연결 (끊김 0) |
| 구분 단서 | 밀도 | 이음선 유무 |

- 스택 3층 위에 현재 블록이 얹힌 프레임을 덤프해, 현재 블록은 3행 연속(내부 틈 0)이고 스택은
  2행 + 1행 틈이 반복되는 것을 확인
- 착지 가로선(18행)과 바로 아래 스택(19행) 사이에 빈 핀 행이 1줄 유지되는 것을 확인 —
  선이 표면에 녹지 않음
- 낙하 5종·퍼즐 4종 질감을 보드 전 좌표에서 생성해 40×60 범위 이탈 0건, 빈 프레임 0건
- 인라인 스크립트 `node --check` 통과, jsdom 실행 시 런타임 오류 0건
- 1차 수정 회귀 없음: 스폰 y=0 유지, 250ms마다 좌우 조작 시 4.5초 고정, 무조작 시 2.6초 고정

### 실제 닷패드에서 확인이 필요한 항목

1. 층마다 1핀 틈이 손끝에서 "층 구분"으로 읽히는지, 아니면 그냥 거친 면으로만 느껴지는지
2. 현재 블록이 스택 위에 얹혔을 때 이음선 유무만으로 경계를 찾을 수 있는지. 어렵다면 현재
   블록 주위 1핀을 비우는(moat) 방식이 대안
3. 퍼즐 모드 5×3(15핀) 대 5×5(25핀) 대비가 낙하 모드보다 약한데 충분한지
4. 스택이 높아졌을 때 틈의 개수로 높이를 가늠하는 것이 실제로 쓸모 있는지

## 실기 1차 피드백 수정 (촉각 우선순위·스폰·바닥 고정) — 검증 결과

jsdom 헤드리스 하네스로 `index.html`을 실제 실행해서 검증했습니다(Canvas/AudioContext/TTS/
Bluetooth 스텁, `performance.now()`와 `dropLoop()`를 직접 구동해 시간을 제어). 수정 전 파일에
동일한 하네스를 돌려 대조했습니다.

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 250ms마다 좌우 조작 시 바닥 고정 | 40초 동안 고정 안 됨 | 4.5초에 고정 (조작 18회) |
| 조작 없이 대기 시 고정 | 2.5초 | 2.6초 (회귀 없음) |
| "곧 고정됩니다" 예고 음성 | 없음 | 있음 |
| 스폰 시 온전히 출력되는 블록 | T는 1/4칸, O·S·Z·J·L은 2/4칸 | 7종 전부 4/4칸 (48/48핀) |
| 낙하 셀 고정 대 현재 대비 | 6핀 대 10핀 (역전) | 4핀 대 12핀 (3.0배) |
| 퍼즐 셀 고정 대 현재 대비 | — | 9핀 대 25핀 (2.8배) |

- 인라인 스크립트 `node --check` 통과, 초기화부터 플레이까지 런타임 오류 0건
- `getDotpadFrame()`이 기존과 동일한 600 hex를 반환. 블록 2개 고정 + 현재 블록 + 착지선 상태에서
  올라간 핀 96개가 예상치(고정 8칸×4 + 현재 4칸×12 + 착지 4칸×4)와 일치
- 낙하 5종·퍼즐 4종 질감을 보드 전 좌표에서 생성해 40×60 범위 이탈 0건, 빈 프레임 0건
- 보드 최하단 20행이 핀행 57·58·59에 정확히 매핑됨(바닥 도달 확인)
- L 블록 4칸이 이음선 없이 하나의 연속된 덩어리로 출력되는 것을 프레임 덤프로 확인

### 실제 닷패드에서 확인이 필요한 항목

1. 고정 블록 4핀이 스택 표면을 훑을 때 충분히 감지되는지. 너무 약하면 `SOLID_INSET`을 0으로
   내려 6핀(3×2)으로 되돌릴 수 있습니다
2. 현재 블록(꽉 찬 덩어리)과 착지 가로선이 서로 붙어 있을 때(착지까지 1~2칸) 구별되는지
3. 잠금 유예 상한 8회 / 3배가 촉각으로 판단할 시간으로 충분한지, 아니면 답답한지
4. "곧 고정됩니다" 예고가 조작 흐름을 끊지 않는지
5. 스폰이 0행으로 내려온 뒤 게임오버가 체감상 너무 빨라지지 않는지

## 실기 피드백 반영 (난이도별 학습 곡선·촉각 재탐색·위험 펄스·마이크로 덕킹) — 검증 결과

jsdom 기반 헤드리스 시뮬레이션(Canvas/AudioContext/Bluetooth 스텁)으로 검증했습니다. 실제 DotPad
BLE 하드웨어가 없는 환경이라, 촉각·음향이 실제로 어떻게 느껴지는지는 아래 "실기 확인 필요" 항목으로
남겨둡니다.

- `index.html` 인라인 스크립트, `audio-engine.js` 모두 `node --check` 통과
- jsdom 시뮬레이션: 인트로 → 닷패드 없이 시작 → 이동/회전/보관/일시정지 → 핀 미리보기 켜고 F(재탐색)
  → 설정 서랍에서 `새 블록 항상 정지` 토글·배경음악 스타일 변경 → 인트로 복귀 → 난이도 기본 선택 →
  `닷패드 연결하고 시작`(블루투스 미지원 환경, 연결 실패 후에도 정상 시작 확인) → 블록 퍼즐 전환 →
  회전·F 재탐색까지 전 구간 `pageerror` 0건
- `getAccessibilityState()`·`getDotpadFrame()` 진단 API가 기존과 동일한 형식(600 hex, 40×60)으로
  계속 동작
- 오디오 엔진 단독 실행: `duck()`·`microDuck()`·`updateMusicGain()`·`setDanger()`·
  `earcon('piece',{piece:'T'})` 예외 없이 호출 확인. 음성 덕킹(voiceDuckUntil)과 마이크로 덕킹
  (microDuckUntil)이 겹칠 때 음성 쪽이 우선하도록 코드 검토 완료(자동 테스트로는 오디오 그래프 실측
  불가 — 실기에서 청감 확인 필요)
- `buildPortraitFrame()`에서 위험 레일 상시 렌더링 블록을 제거하고, `padDangerEdgePulse()`가
  `updateDanger()`의 레벨 상승 시점에만 호출되는 것을 코드 경로로 확인
- F1+F3(`KeyFunction13`)이 낙하·퍼즐 두 모드 모두에서 기존에 비어 있던 키였음을 이전 기능키 감사
  결과와 대조해 재확인 — 다른 조작과 충돌 없음

### 실제 닷패드에서 확인이 필요한 항목

1. 기본 난이도에서 같은 블록을 두 번째 만났을 때 정지 없이 바로 떨어지는 전환이 자연스러운지
2. 이어콘만 남았을 때(4회차 이후) 여전히 블록을 구별할 수 있는지, 세 번이 학습에 충분한 횟수인지
3. F1+F3 재탐색 시 현재 블록 2회 강조와 착지점 1회 강조가 시간·강도 면에서 구별되는지
4. 위험 진입 2회 펄스가 지속 표시보다 오히려 놓치기 쉽지는 않은지 (특히 손이 보드 중앙에 있을 때)
5. 스폰·회전·고정 마이크로 덕킹이 실제 이어폰/스피커에서 조작음을 또렷하게 만드는지, 아니면
   거의 체감되지 않는지
6. `닷패드 연결하고 시작` 흐름에서 브라우저 기기 선택 창이 뜬 뒤 실제 연결까지 걸리는 시간이
   부스 시연에서 부담스럽지 않은지

## 단독 파일 실행 — 검증 결과

| 환경 | 결과 |
|---|---|
| 형제 파일과 함께 (저장소·GitHub Pages) | 콘솔 깨끗, 게임 정상 |
| `index.html` 단독 | `pageerror` 0건, 게임 진행 가능(무음·닷패드 없음), 원인 안내 2줄만 출력 |

이전에는 단독 실행 시 `audio.setMode is not a function`으로 게임이 중단됐고, SDK 실패가
`TypeError: ... does not resolve to a valid URL`로만 표시돼 원인을 알기 어려웠습니다.

## 촉각 테트리스 1~4 — 검증 결과

- 블록 이어콘 7종(I/O/T/L/J/S/Z) 및 인자 누락 시 기본값 모두 예외 없이 재생
- 기존 이어콘 13종 회귀 없음
- 난이도 전환: 클릭·키보드 1/2/3 모두 `aria-selected`와 시작 버튼 라벨이 따라감,
  `settings.speed`가 `turn`/`tactile`/`standard`로 저장됨
- 연습 모드에서 2.5초 대기해도 블록이 내려오지 않음(중력 없음) 확인
- 홈 복귀 시 포커스가 선택된 난이도 카드로 이동
- 새 블록 확인 시간: 꺼짐일 때 1초 안에 낙하, 켜짐일 때 1초간 정지 후 낙하 (도전 속도 기준 대조)
- 저장된 속도로 인트로 난이도 복원됨
- 1440×900 / 390×844 오버플로우 0, 44px 미만 터치 영역 0
- pageerror 0건, 실패 요청 0건

### 실기 확인이 필요한 항목

1. 블록 이어콘 7종이 실제로 서로 구분되는지, 특히 S와 Z(패닝 방향만 반대)
2. 위험 2단계 핀 밀도 변화가 손끝에서 1단계와 구분되는지
3. 새 블록 확인 시간 1.4초가 적절한지 (짧으면 못 느끼고 길면 답답함)
4. 연습 모드에서 중력이 없다는 사실이 안내 없이도 이해되는지

## 퍼즐 트레이 레이아웃 · 슈퍼닷 TTS — 검증 결과

- 퍼즐 라이브러리 13종 전체를 `renderMini()` 계산식으로 대조해 셀 간격 포함 최대 축 42px 이하 확인
- 퍼즐 모드 `놓을 블록` 트레이를 1440×900, 900×700, 562×620, 390×844에서 재검수
- 1200×900, 900×700, 600×664, 390×844에서 랜덤 트레이 80회 반복 렌더링 —
  프리뷰-텍스트 겹침 0건, 버튼 외부 이탈 0건, 문서 오버플로우 0건
- 사용자 제보와 같은 `세로 세 칸 / 세로 세 칸 / 모서리` 조합을 강제 재현해 카드 내부 수납 확인
- `.mini` 실제 바운딩 박스 기준으로 프리뷰-텍스트 겹침 0건, 버튼 외부 이탈 0건
- 네 뷰포트 모두 문서 오버플로우 가로·세로 0, 실패 요청 0건, `pageerror` 0건, 콘솔 에러 0건
- 인트로, 낙하 시작, 퍼즐 시작, 설정 서랍을 1440×900, 900×700, 390×844에서 재검수 —
  본 화면 주요 UI의 뷰포트 외부 이탈 0건. 설정 서랍 내부 카드는 `overflow:auto` 영역 안에서
  아래로 이어지는 정상 스크롤 콘텐츠로 확인
- 기본 음성 안내 방식은 `superdot`; 슈퍼닷 모드에서 `status[aria-live]=off`,
  `criticalLive[aria-live]=off`, `criticalLive[role]=status` 확인
- 스텁 주입 검수에서 `SUPERDOT_TTS === TW_TTS`, `setEnabled(true)`, `setLang('ko')`,
  게임 시작 안내 `speak()` 호출 확인
- 단독 실행에서는 슈퍼닷 TTS 코어를 브라우저 음성 우선으로 설정해 없는 서버 `/api/tts`를 선시도하지 않음

## 닷패드 기능키 매핑 감사

SDK 모듈을 실제로 로드해 `KeyCodes` 실물과 대조했습니다. 하드웨어 없이 확인 가능한 범위는
**키 상수 존재 여부, 분기 도달 가능성, 상태 가드**까지이며 실제 입력 인식은 실기 확인이 필요합니다.

### 낙하 모드

| 키 | 동작 |
|---|---|
| F1 / F2 | 왼쪽 / 오른쪽 이동 |
| F3 | 회전 |
| F4 | 한 칸 내리기 |
| F1+F2 | 보관 |
| F2+F3, 오른쪽 패닝 | 즉시 낙하 (2회 확인) |
| 왼쪽 패닝 | 간단 상태 |
| F3+F4 | 상세 상태 |
| F2+F4 | 다음·보관 읽기 |
| 전체 패닝 | 촉각 표면 스캔 |
| RPF4 | 일시정지 |
| LPF1 | 다시 시작 (2회 확인) |
| **F1+F4** | **설정 메뉴 (이번에 연결)** |

### 블록 퍼즐

| 키 | 동작 |
|---|---|
| F1 / F2 | 왼쪽 / 오른쪽 |
| F3 / F4 | 위 / 아래 |
| 왼쪽 패닝 | 회전 |
| 오른쪽 패닝 | 블록 선택 |
| 전체 패닝, F2+F3 | 놓기 |
| F1+F2 | 상세 상태 |
| RPF4 | 간단 상태 |
| LPF1 | 다시 시작 (2회 확인) |
| **F1+F4** | **설정 메뉴 (이번에 연결)** |

### 화면별 진입점

- 인트로: F1 낙하 모드, F2 블록 퍼즐, F3 도움말, F4 설정
- 결과: F1·LPF1 다시 시작, F2 인트로, F4 설정 (F3 미할당)
- 설정 메뉴: F1 이전, F2 다음, F3 변경, F4 닫기

### 확인 결과

- 참조하는 키 15종 중 `LPF4`만 SDK 미정의. 나머지 14종 전부 존재
- 이동·회전·보관·놓기 함수 모두 `mode / running / paused / over / scanMode / isAnimating` 가드 보유 —
  일시정지·스캔 중 입력이 들어가지 않음
- 미사용 키: 두 모드 공통 `KeyFunction13`, 퍼즐 모드 추가로 `KeyFunction24`·`KeyFunction34`

### 실기 확인이 필요한 항목

1. F1+F4 동시 입력이 F1 또는 F4 단독으로 오인되지 않는지 (특히 빠른 조작 중)
2. 촉각 턴제에서 중력이 없으므로, F4로 내리지 않으면 블록이 멈춰 있는 것이 혼란스럽지 않은지
3. F2+F3 두 번 확정과 오른쪽 패닝 두 번 확정의 1.8초 창이 실제 조작 속도에 맞는지

F1+F4는 앞서 남긴 F2+F3 확인 항목과 같은 종류의 동시 입력 펌웨어 판정 리스크입니다.
코드상 `KeyFunction14` 분기는 도달 가능하지만, 안정 동작 여부는 실기 확인 전까지 확정하지 않습니다.

## P0 소스 반영 — 검증 결과

- DOM 중복 없음: `#introScreen` 1, `#startSelectedBtn` 1, `#board` 1, 타이틀 타일 9
- 실패 요청 0건, `pageerror` 0건, 콘솔 에러 0건 (`Blind-first upgrade skipped` 소멸)
- 설정 시트에 음성 안내 방식 셀렉트 노출, 낙하 방식 5종(`tactile,turn,relaxed,standard,challenge`)
- 낙하 방식 기본값 `tactile` 적용 확인
- 기본값(슈퍼닷 TTS)에서 `status[aria-live]=off`, `criticalLive[role]=status` — 합성음과 겹쳐 읽지 않음
- 즉시 낙하: `Space` 1회로는 점자 라인 불변, 2회째에 확정됨
- 닷패드 프레임 600 hex 유지, `kind==='flash'`·`enterPadSettings` 코드 존재
- 소리 토글: 끄면 시트 3개 모두 "꺼짐", 재클릭 시 복원 (`syncSettingUI`와 양방향 연결)
- 임베드 계약 `ready`/`resize`/`gameStart`/`exit`, `tw:pause`·`tw:resume` 정상
- 임베드일 때만 `/tts.js`·`/api/tts` 요청 발생(정상), 단독 실행에서는 0건

### 실제 닷패드에서 확인이 필요한 항목

1. 촉각 턴제에서 바닥 안내 → F2+F3 두 번 확정까지의 흐름이 실제 손으로 자연스러운지
2. 설정 메뉴 7개 항목을 F1/F2로 오가는 음성 길이가 부담스럽지 않은지
3. 고정 블록(작고 촘촘) vs 움직이는 블록(큰 빈 테두리) 대비가 손끝에서 실제로 구분되는지

## 블록 타이틀 · 콘솔 — 검증 결과

### 콘솔

서브패스 배포(`/dot-blocks/index.html`)로 재현 환경을 만들어 확인

- 실패 요청 0건 (이전: `/tts.js` 1건, `/api/tts` 2건)
- `pageerror` 0건
- `DotBlocksAudioEngine is not a constructor` 재현 안 됨

### 타이틀

| 타일 | 색 | 다크 잉크 글자 대비 |
|---|---|---|
| B | `#ffd129` | 13.53:1 |
| O | `#39cf78` | 9.74:1 |
| O | `#17b9f5` | 8.73:1 |
| L | `#ff8629` | 8.16:1 |
| S | `#f04455` | 5.32:1 |
| D·C | `#287cff` | 5.09:1 |
| T·K | `#8d42ef` | 3.90:1 (큰 텍스트 3:1 기준 통과, 88px) |

- 접근성 트리에서 heading 이름이 `"닷 블록스, DOT BLOCKS"` 하나만 노출됨
- 낱글자 9개 모두 `aria-hidden`
- 1440/700/390 세 뷰포트 오버플로우 0. 셀 88px → 58px → 49px, 720px 이하 2줄 전환 확인

### 미해결 — 결정 필요

`blind-first-p0.js`의 런타임 업그레이드가 **이전부터 동작하지 않고 있었습니다.** 배포 중인 버전
(`3440eb4`)에서도 동일하게 재현되며, 콘솔에 `Blind-first upgrade skipped`가 남습니다.

- 직접 원인: `countText()`가 부분 문자열로 세는데, `'initial speech mode sync'` needle(2칸 들여쓰기)이
  같은 스크립트가 앞서 주입하는 `syncSettingUI` 본문(4칸 들여쓰기)에도 걸려 카운트가 2가 됨
- needle을 개행으로 앵커링하면 패치는 통과하지만, **그러면 DOM이 두 벌로 복제됩니다.**
  파서가 살아 있는 동안 호출된 `document.open()`은 사양상 무시되어, 이어지는 `document.write()`가
  문서를 교체하지 않고 덧붙이기 때문입니다 (타이틀 타일 18개, `#startSelectedBtn` 2개 확인)
- 즉 이 계층은 현재 구조로는 성공해도 깨집니다. 지금은 패치가 실패해 건너뛰는 덕에 게임이 정상 동작 중
- 선택지: (a) P0 기능을 `index.html`에 직접 반영하고 이 스크립트 제거, (b) 재작성 대신 런타임 DOM
  패치로 전환, (c) 현 상태 유지하고 콘솔 경고만 낮춤

## 화면 구조 재설계 · 공통 규칙 — 검증 결과

### 레이아웃

- 1440×900 / 1280×720 / 390×844 세 뷰포트에서 문서 오버플로우 가로·세로 모두 0
- 44px 미만 터치 영역 0개
- 보드 크기: 데스크톱 337×634, 1280×720 243×457, 모바일 272×512
- 모바일 세로 순서에 겹침 없음 — 보드 하단 740 → 안내 753 → 컨트롤 788 → 뷰포트 844
- 모바일 HUD 배치 확인: 다음 블록(1행 1열) / 기록(1행 2열) / 보드(2행 전체)

### 보드 프레임 정합

스크린샷 픽셀을 직접 샘플링해 베젤과 캔버스 경계를 측정

- 가로 중앙선: 좌측 베젤 끝 568px, 캔버스 시작 570px / 캔버스 끝 851px, 우측 베젤 시작 852px
- 세로 중앙선: 상단 베젤 끝 173px, 캔버스 시작 175px / 캔버스 끝 754px, 하단 베젤 시작 755px
- 네 변 모두 1px 이내

### 색상 대비 (WCAG 2.1 AA, 4.5:1 기준)

| 조합 | 비율 |
|---|---|
| 본문 잉크 `#f5f7fa` / 배경 `#08090b` | 18.56:1 |
| 보조 텍스트 `#aeb4bd` | 9.55:1 |
| 흐린 텍스트 `#747b86` | 4.67:1 |
| 액센트 `#ff5b22` / 배경 | 6.42:1 |
| 프라이머리 라벨 `#140801` / 액센트 | 6.36:1 (hover 7.4:1) |
| 포커스 링 `#ffc928` / 배경 | 12.94:1 |
| 고정 블록 `#d8dee7` / 보드 `#0a0c0f` | 14.47:1 |
| 조작 중 블록 `#ff5b22` / 보드 | 6.31:1 |

### 호스트 연동

부모 프레임 하네스에 `?embed=1`로 iframe 삽입 후 실측

- 송신 순서 `ready` → `resize` → `gameStart` → `exit` 확인
- `ready` 페이로드 `{source:'dotarcade', game:'dot-blocks', type:'ready', modes:['drop','puzzle'], lang:'ko'}`
- `gameEnd` 페이로드에 `mode`·`score`·`lines`·`level` 포함 확인 (실제 게임오버까지 진행해 확인)
- `tw:pause` 수신 시 일시정지 라벨 "계속", `tw:resume` 수신 시 "일시정지"로 복귀
- 소리 토글: `aria-pressed` true→false, 설정 서랍 3개 항목 모두 "꺼짐", 재클릭 시 복원
- 단독 실행(부모 프레임 없음)에서 pageerror 0건

### 코드 검증

- 인라인 JavaScript `node --check` 통과
- 인트로·플레이·설정 서랍·일시정지·결과·촉각 스캔 전 화면 렌더링 후 pageerror 0건

### 실제 닷패드에서 확인이 필요한 항목

1. 밝은 베젤 아트워크가 저시력 사용자에게 보드 경계로 읽히는지 (화면 대비는 통과했으나 실사용 판독은 별개)
2. 하단 조작 안내 레일이 컨트롤 도크와 시각적으로 혼동되지 않는지
3. 호스트 임베드 상태에서 `tw:pause` 수신과 닷패드 핀 상태 유지가 함께 동작하는지

## 기능키 확정 동작 · 타이틀 · 점자 라인 — 검증 결과

- 낙하/퍼즐 두 모드의 키 분기 순서를 정적 분석해 `KeyFunction23`이 도달 가능함을 확인
  (앞선 분기에 catch-all 없음). 모드별 중복 매핑 없음
- `F2+F3`과 기존 패닝키가 **동일 함수**(`hardDrop()` / `placePuzzle()`)를 호출함을 코드로 확인
- 확정 동작 후 점자 라인이 `L #4 #19 (다음 I)` → `I #4 #15 (다음 S)`로 갱신되어
  블록이 실제로 고정되고 다음 블록이 올라오는 것을 확인
- h1 접근성 이름이 "DOT BLOCKS" 한 번만 낭독됨 (데스크톱·모바일 동일)
- 점자 비트 레이아웃을 기존 상수 6개와 대조 검증 후 블록 글자 7종 산출
- 닷패드 프레임 600 hex / 60×40 유지, 페이지 에러 없음

### 실제 닷패드에서 확인이 필요한 항목

1. **F2+F3 동시 입력이 실기 펌웨어에서 안정적으로 인식되는지** — 특히 빠른 연타 시
   F2 또는 F3 단독 입력으로 오인되어 의도치 않은 이동/회전이 일어나지 않는지
2. 20칸 점자 라인에 토큰 4개가 손끝 한 번에 스캔되는 분량인지
   (부담되면 다음 블록을 빼고 `F2+F4`에만 남기는 안으로 되돌릴 수 있음)
3. 수표 뒤 숫자와 블록 글자가 인접했을 때 혼동 여부 — `j`(2·4·5점)와 숫자 `0`은 같은 점형
4. 확정 키가 두 개(F2+F3, 패닝)인 것이 혼란스럽지 않은지

## 닷패드 숙련자 대응 — 검증 결과

- **점자 라인 실시간 갱신 확인** (진단 API `getAccessibilityState().brailleLine` 디코딩)
  - 낙하: `#4 #19` → 우로 이동 시 `#5 #19` `#6 #19` `#7 #18` `#8 #18`, 소프트드롭 후 `#8 #12`
  - 퍼즐: `#1 #1 o` → 커서 이동 시 `#1 #2 o` `#1 #3 o` `#1 #4 o` (o=놓을 수 있음, x=겹침)
  - 20셀(40 hex) 유지
- **점자 숫자 0 표준 대조**: 1~9는 기존에도 표준과 일치, 0만 `1·3·5·6점`으로 오류 → `2·4·5점`으로 교정
- **음성 길이 실측**: 간단 22자 / 다음·보관 19자 / 상세 135자 (기존 단일 안내 약 110자)
- 전체 키 스윕(←→↑↓ C S D V Q P Space) 후에도 `getDotpadFrame()` 600 hex, 60×40 유지, 페이지 에러 0건

### 실제 닷패드에서 확인 필요

1. 점자 라인의 `#열 #착지칸수`를 손가락으로 읽으며 동시에 촉각 보드를 탐색하는 것이 실제로 편한지
   (두 손 사용 가정 — 한 손은 점자 라인, 한 손은 핀 면)
2. 퍼즐 모드 `o`/`x` 표시가 촉각으로 즉시 구분되는지
3. 간단 상태(22자)만으로 충분한지, 아니면 착지 행 번호도 필요한지
4. `F2+F4`(다음·보관) 동시 입력이 기기 펌웨어에서 안정적으로 인식되는지

## 순수 배경 · 문구 축약 — 검증 결과

- 배경 5종 모두 WebP 변환 후 적용, 실패한 리소스 요청 없음 (파일 크기 29~78KB)
- 배경별 명도/분산을 5×6 격자로 측정해 UI 배치 결정:
  - 인트로 가로: 좌측 열 명도 18~96(저분산) → 문구 좌측 유지
  - 인트로 세로: **상단 행 23~31(저분산)** → 문구 상단 정렬로 변경
  - 설정·결과: 우측 열 9~26 → 서랍 위치와 부합
- 실제 렌더링에서 배경 적용 확인: 모바일 인트로 `intro-bg-mobile.webp` + `align-items:flex-start`, 제목 y123~206(빈 영역), 모바일 플레이 `play-bg-mobile.webp`
- 문구 영역 배경 명도 측정으로 가독성 확인 (플레이 p95=84, 설정 p95=211 → 흰 텍스트 충분히 판독 가능)
- 블록 색상은 테트리스 표준 배색과 겹치지 않는 기존 독자 팔레트를 그대로 유지 (변경 없음)
- CSS 중괄호 균형, 인라인 JavaScript `node --check` 통과, 데스크톱·모바일 페이지 에러 없음

### 실제 확인이 필요한 항목

1. 새 배경 위에서 캔버스 보드 테두리가 충분히 분리되어 보이는지 (특히 모바일 중간 밝은 띠)
2. 축약된 인트로 문구가 처음 접하는 사용자에게 충분한 안내인지 — 상세 안내는 "게임 방법 듣기"로 이동됨

## 레트로 칩튠 배경음악 — 검증 결과

- 편곡 검산: 8마디 × 16스텝 = 128스텝으로 정확히 떨어짐 (마디별 합계 전부 16 확인)
- 모의 AudioContext에서 128스텝 전체를 순회해 멜로디 온셋 37개가 모두 예상 스텝에 안착함을 확인
- 실제 Chromium에서 스케줄된 주파수 검증:
  - 1마디 `659(E5) 494(B4) 523(C5) 587(D5) 523(C5) 494(B4)`
  - 2마디 `440(A4) 440(A4) 523(C5) 659(E5) 587(D5) 523(C5)` → 코로베이니키 원곡 선율과 일치
  - 베이스 `165(E3) / 247(B3)` 루트–5도 교대 정상
- 설정 UI: 셀렉터 렌더링, 선택 시 엔진 즉시 반영, `localStorage` 저장, 새로고침 후 값 유지 확인
- 스타일 전환 시 `step`을 0으로 초기화해 루프가 깨끗하게 재시작됨
- HTML ID 104개 중복 없음, 인라인 JavaScript·`audio-engine.js` `node --check` 통과, 페이지 에러 없음

### 실제 사용자 테스트가 필요한 항목

1. 레트로 음악이 방향 효과음(스테레오 팬)과 위험 경고음을 가리지 않는지 — **시각장애인 사용자에게는 오디오가 주 정보 채널이므로 최우선 확인 항목**
2. 음성 안내 중 자동 감쇠(18%)가 칩튠에서도 충분한지 (기존 앰비언트 대비 배음이 많아 체감이 다를 수 있음)
3. 레벨 상승·위험 구간에서 템포가 빨라질 때 집중에 방해가 되지 않는지
4. 이어폰/스피커 각각에서 베이스(E3 165Hz)가 과하지 않은지
5. 장시간 플레이 시 128스텝(8마디) 루프의 반복 피로도

## 아케이드 게임감 강화 — 검증 결과

- 신규 함수 `showFx`/`bumpBoard`/`popStat` 및 호출부 이름 충돌 없음을 정적 분석으로 확인 (스코프별 카운트 일치)
- `lockDrop`(Drop)과 `placePuzzle`(Puzzle) 모두에서 기존 점수 계산식·오디오 타이밍·TTS 안내 문구는 변경 없이, 연출 트리거만 추가
- 헤드리스 Chromium에서 `#fxPop`/`#canvasWrap`/`#scoreValue`에 신규 클래스를 직접 토글해 애니메이션이 콘솔 에러 없이 렌더링되는 것을 확인
- `prefers-reduced-motion` 전역 리셋 규칙이 신규 애니메이션도 모두 포괄 (`fx-pop`만 모션 없이 즉시 표시되도록 별도 예외 처리)
- CSS 중괄호 균형, 인라인 JavaScript `node --check` 통과

## UI/UX 세련화 패스 — 작업 보고 (UI 작업 표준 원칙 기준)

- **재사용한 기존 토큰**: `--accent`, `--accent2`, `--ok`, `--danger`, `--muted` 그대로 유지
- **신규 공통 토큰**: `--cta-1`/`--cta-2`/`--cta-glow`(3곳에서 드리프트되던 CTA 그라데이션 통일), `--panel-deep`/`--panel-deep-2`/`--line-blue`/`--line-blue-soft`(반복되던 패널·보더 블루 통일), `--ease`/`--ease-out`(트랜지션 타이밍)
- **아이콘**: 유니코드 글리프(Ⅱ, ★) 2건을 발견해 기존 인라인 SVG 아이콘 시스템과 통일된 스타일로 교체 (Tabler 스타일 stroke 2px 유지, 신규 임의 SVG 없음)
- **중복 제거**: 동일 의도의 CTA 그라데이션 3벌(인트로 시작 버튼/플레이 헤더 로고/오버레이 확인 버튼)을 토큰 1벌로 통합
- **접근성 검증 결과**:
  - 색상 대비: 화이트 텍스트 on CTA 그라데이션 시작색 2.91:1 → `#0a86dd` 조정 후 3.84:1 (WCAG AA 큰글자 기준 3:1 충족)
  - 터치 타깃: `.btn`(39→44px), 홈/메뉴 버튼(40→44px), 드로어 닫기(40→44px), 플레이 컨트롤바 버튼(40→44px) 모두 44px 기준 충족. `max-height:720px and min-width:641px` 압축 쿼리(34px)는 터치가 아닌 데스크톱/노트북 좁은-세로 창 전용으로 판단해 기존 압축 규칙 유지
  - `prefers-reduced-motion`: 신규 트랜지션/애니메이션 전체를 포괄하도록 리셋 규칙 확장, 코드 검증 통과
  - 키보드 포커스: 기존 `body.kbd` 포커스 링 규칙 변경 없음
- **모션**: 신규 진입 애니메이션(인트로/플레이/드로어/오버레이)은 전부 `[hidden]` 속성 기반이라 JS 변경 없이 CSS만으로 구현 — 게임 로직 손상 위험 없음
- **코드 검증**: CSS 중괄호 균형(301/301), 인라인 JavaScript·`audio-engine.js`·`tts.js` `node --check` 전부 통과

## 자동/코드 검증 완료

- `index.html` 인라인 JavaScript `node --check` 통과
- `audio-engine.js` `node --check` 통과
- DOM/API 스텁 기반 초기화 및 입력 시뮬레이션 통과
- 낙하 모드 시작, 이동, 회전, 보관, 상태 읽기, 표면 스캔, 하드 드롭 호출 통과
- 블록 퍼즐 시작, 이동, 회전, 선택, 배치 호출 통과
- 음성/효과음/BGM/잠금 유예 설정 전환 통과
- 반복 하드 드롭 스트레스 호출에서 예외 없음
- `window.getPortraitFrame()`: 60행 × 40열
- `window.getDotpadFrame()`: 600 hex 문자 = 300 bytes
- `window.getAccessibilityState()` 진단 API 제공
- HTML ID 100개 중복 없음, 핵심 스크립트 참조 ID 누락 없음
- 절차형 오디오 엔진을 모의 AudioContext에서 전체 이어콘 호출 검증

## 화면 분리 및 반응형 검증

- Chromium 기반 Playwright UI 시뮬레이션에서 인트로 → 플레이 → 설정 → 일시정지 → 계속 → 인트로 흐름 통과
- 데스크톱 1440×900: 인트로, 낙하 플레이, 설정 서랍 렌더링 확인
- 모바일 390×844: 인트로, 블록 퍼즐 플레이와 3개 퍼즐 트레이 렌더링 확인
- `window.getDotpadFrame()` 600 hex와 `getAccessibilityState().frameLength` 600 유지
- 실행 중 JavaScript page error 및 console error 없음
- 기본 블록은 Canvas, 배경은 WebP, 특수 아이템은 투명 SVG로 역할 분리

## 실제 DotPad에서 반드시 확인할 항목

이 실행 환경에는 DotPad BLE 하드웨어가 없으므로 아래 항목은 실제 사용자 테스트가 필요합니다.

1. `cw`와 `ccw` 중 실제 세로 거치 방향과 일치하는 기본값
2. 고정 블록/현재 블록/예상 착지 세 질감의 구별 가능성
3. 4×3핀 셀에서 현재 블록 테두리가 손끝에 충분히 명확한지
4. 표면 스캔이 10개 열 높이를 빠르게 이해하는 데 도움이 되는지
5. 상단 위험 경고점이 게임 블록으로 오인되지 않는지
6. F1+F2, F3+F4, RPF4 동시/보조 키 코드가 기기 펌웨어별로 일치하는지
7. BLE 갱신 중 빠른 좌우 이동에서 프레임 누락이 없는지
8. 음성 안내가 재생되는 동안 음악 감쇠가 충분한지
9. 이어폰 사용 시 좌우 방향 효과음이 명확한지
10. 실제 시각장애인 사용자 3명 이상이 화면 없이 10분 이상 플레이 가능한지

## 권장 사용자 테스트 과업

### 낙하 모드

- I 블록을 왼쪽 끝과 오른쪽 끝까지 이동
- 블록을 2회 회전하고 예상 착지 위치 확인
- 블록 보관 후 다른 블록과 교체
- 표면 스캔으로 가장 높은 열 찾기
- 한 줄 완성 후 줄 삭제 피드백 설명
- 위험 경고 후 높이를 낮추기

### 블록 퍼즐

- 3개 블록을 차례로 선택
- 겹치는 자리의 X 질감 찾기
- 가로줄과 세로줄 각각 1회 삭제
- 상세 상태 읽기로 가장 많이 찬 행 찾기

## 합격 기준 제안

- 조작 성공률 90% 이상
- 현재 블록/고정 블록/착지 표시 구분 정확도 85% 이상
- 도움 없이 첫 줄 삭제까지 8분 이내
- 상태 읽기 요청 후 현재 열 범위 응답 정확도 90% 이상
- 게임 종료 이유를 모든 참가자가 설명 가능
